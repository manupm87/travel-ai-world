import UsersClientPage from "./UsersClientPage";

/** `/admin/users/` — a static shell over every account; nothing here reads the query. */
export default function AdminUsersPage() {
  return <UsersClientPage />;
}
