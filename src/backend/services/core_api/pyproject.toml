[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "core-api"
version = "0.1.1"
description = "Kyrian World core API: Google auth, users and trip CRUD on PostgreSQL"
requires-python = ">=3.12"
dependencies = [
    "travel-common",
    "fastapi[standard]>=0.141.1",
    "sqlalchemy>=2.0.52",
    "asyncpg>=0.31.0",
    "alembic>=1.19.2",
    "email-validator>=2.3.0",
    "httpx>=0.28.1",
]

[tool.uv.sources]
travel-common = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["core_api"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
addopts = "--import-mode=importlib"
testpaths = ["tests"]
