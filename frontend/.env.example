# Google OAuth Configuration
# Get yours at: https://console.cloud.google.com/
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# Backend API URL (optional — omit for static-only GitHub Pages deployment)
# Set this when running with the FastAPI backend locally or in production
NEXT_PUBLIC_API_URL=http://localhost:8000
